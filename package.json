{
  "name": "emmaline",
  "version": "0.1.0",
  "description": "AI Phone Call Buddy - A hands-free voice assistant with transcription and note-taking",
  "private": true,
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:mobile": "npm --prefix mobile start",
    "build:mobile:dev": "npm --prefix mobile run build:dev",
    "build:backend": "npm --prefix backend run build",
    "lint": "npm --prefix backend run lint && npm --prefix mobile run lint"
  },
  "devDependencies": {
    "eas-cli": "^20.2.0"
  }
}
